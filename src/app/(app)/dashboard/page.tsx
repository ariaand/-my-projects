import { DashboardClient } from "@/components/dashboard/dashboard-client";

export const metadata = { title: "Dashboard — Henry" };

export default function DashboardPage() {
  return <DashboardClient />;
}
