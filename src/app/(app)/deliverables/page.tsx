import { DeliverablesClient } from "@/components/deliverables/deliverables-client";

export const metadata = { title: "Deliverables — Henry" };

export default function DeliverablesPage() {
  return <DeliverablesClient />;
}
